
📝 سوالات چندگزینه‌ای (Multiple Choice)

    کدام تگ HTML برای تعریف یک پاراگراف استفاده می‌شود؟

        الف) <div>

        ب) <p>

        ج) <span>

        د) <section>

    کدام ویژگی CSS برای تغییر رنگ متن استفاده می‌شود؟

        الف) background-color

        ب) font-size

        ج) color

        د) text-align

    کدام تگ برای درج تصویر در HTML استفاده می‌شود؟

        الف) <img>

        ب) <image>

        ج) <pic>

        د) <src>

    کدام ویژگی CSS برای تنظیم فاصله داخلی یک عنصر استفاده می‌شود؟

        الف) margin

        ب) padding

        ج) border

        د) width

    کدام تگ برای ایجاد یک لینک در HTML استفاده می‌شود؟

        الف) <link>

        ب) <a>

        ج) <href>

        د) <url>

    کدام ویژگی CSS برای تعیین اندازه فونت استفاده می‌شود؟

        الف) font-style

        ب) font-weight

        ج) font-size

        د) text-size

    کدام تگ برای ایجاد یک لیست نامرتب در HTML استفاده می‌شود؟

        الف) <ol>

        ب) <ul>

        ج) <li>

        د) <list>

    کدام ویژگی CSS برای تغییر پس‌زمینه یک عنصر استفاده می‌شود؟

        الف) background-color

        ب) color

        ج) border

        د) opacity

    کدام تگ برای ایجاد یک جدول در HTML استفاده می‌شود؟

        الف) <table>

        ب) <tr>

        ج) <td>

        د) <th>

    کدام ویژگی CSS برای مخفی کردن یک عنصر استفاده می‌شود؟

        الف) visibility: hidden

        ب) display: none

        ج) هر دو مورد بالا

        د) هیچ‌کدام

✍️ سوالات جای‌خالی (Fill in the Blanks)

    برای لینک دادن یک فایل CSS خارجی به فایل HTML، از تگ <link> با ویژگی _____ استفاده می‌کنیم.

    برای مشخص کردن عنوان اصلی صفحه، از تگ _____ استفاده می‌شود.

    برای ایجاد یک خط افقی در صفحه HTML، از تگ _____ استفاده می‌کنیم.

    برای قرار دادن یک تصویر با آدرس "image.jpg" و متن جایگزین "نمونه تصویر"، از تگ _____ استفاده می‌شود.

    برای تعیین رنگ پس‌زمینه به رنگ آبی در CSS، از دستور _____ استفاده می‌کنیم.

    برای ایجاد یک لیست مرتب با اعداد در HTML، از تگ _____ استفاده می‌شود.

    برای تعیین اندازه فونت به 16 پیکسل در CSS، از دستور _____ استفاده می‌کنیم.

    برای ایجاد یک فرم در HTML، از تگ _____ استفاده می‌شود.

    برای ایجاد یک سلول در جدول HTML، از تگ _____ استفاده می‌شود.

    برای اعمال استایل به همه عناصر <p> در CSS، از انتخابگر _____ استفاده می‌کنیم.